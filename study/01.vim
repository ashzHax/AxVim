" sends message (leaves no log)
echo "Hello World!\r"

" sends message (leaves log), is like :execute
echom "Hello again, World!\n"

" can review all output messages
messages

" highlighting set by the echohl command
echohl

" re-draws the screen? 
redraw

" Exercise
echo ">^.^<"
